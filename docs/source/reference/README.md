# Reference

This section is the exact public contract of `langchain-goodmem`.

Use it when you need:

- constructor and method signatures
- documented input and return semantics
- exception behavior
- the precise meaning of GoodMem-specific fields such as chunk IDs, memory IDs,
  `_goodmem_score`, and metadata filters

If you are new to the package, read the guides first and then come back here
for exact API details.

```{toctree}
:maxdepth: 1

api
```
